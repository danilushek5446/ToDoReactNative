import { StyleSheet } from 'react-native';

export const rootStackStyles = StyleSheet.create({
  barStyle: {
    backgroundColor: '#505050',
    borderRadius: 40,
    overflow: 'hidden',
    width: 200,
    alignSelf: 'center',
    margin: 10,
  },
  navigatorStyle: {
    backgroundColor: '#DCDCDC',
  },
});
